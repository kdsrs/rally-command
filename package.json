{
  "name": "rally-command-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "npm install && npm run build -w client",
    "start": "node server/index.js"
  }
}
