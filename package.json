{
  "name": "rally-command-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "npm install && npm install @rollup/rollup-linux-x64-gnu -w client && npm run build -w client",
    "start": "node server/index.js"
  }
}
